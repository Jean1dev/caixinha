diff --git a/node_modules/caixinha-core/dist/src/boxes/Box.d.ts b/node_modules/caixinha-core/dist/src/boxes/Box.d.ts
index d5e133f..4c53523 100644
--- a/node_modules/caixinha-core/dist/src/boxes/Box.d.ts
+++ b/node_modules/caixinha-core/dist/src/boxes/Box.d.ts
@@ -20,6 +20,7 @@ export declare class Box {
     removeMember(member: Member): void;
     memberTryRemoveLoan(member: Member, uuid: string): void;
     removeLoan(uuid: string): void;
+    replaceLoanForRenegotiation(oldLoanUid: string, newLoan: Loan): void;
     getLoanByUUID(loanUUID: string): Loan;
     addBankAccount(keyPix: string | null, qrCode: string | null): void;
     joinMember(member: Member): void;
diff --git a/node_modules/caixinha-core/dist/src/boxes/Box.js b/node_modules/caixinha-core/dist/src/boxes/Box.js
index 994777a..06dd4f1 100644
--- a/node_modules/caixinha-core/dist/src/boxes/Box.js
+++ b/node_modules/caixinha-core/dist/src/boxes/Box.js
@@ -64,7 +64,9 @@ class Box {
                     : [],
                 isPaidOff: loan.isPaidOff,
                 installments: loan.installments,
-                refusedReason: loan.refusedReason
+                refusedReason: loan.refusedReason,
+                disbursed: loan.disbursed,
+                originLoanUid: loan.originLoanUid
             });
         });
         if (jsonBox.performance) {
@@ -126,6 +128,19 @@ class Box {
         this.loans = this.loans.filter(l => l.UUID !== uuid);
         this.sumInCurrentBalance(loan.value);
     }
+    replaceLoanForRenegotiation(oldLoanUid, newLoan) {
+        const oldLoanIndex = this.loans.findIndex(loan => loan.UUID === oldLoanUid);
+        if (oldLoanIndex < 0) {
+            throw new DomainError_1.default('Loan not found');
+        }
+        if (!newLoan.isApproved) {
+            throw new DomainError_1.default('Renegotiated loan must be approved');
+        }
+        if (this.loans.some(loan => loan.UUID === newLoan.UUID)) {
+            throw new DomainError_1.default('Loan already exists');
+        }
+        this.loans.splice(oldLoanIndex, 1, newLoan);
+    }
     getLoanByUUID(loanUUID) {
         const loan = this.loans.find(l => l.UUID === loanUUID);
         if (!loan)
@@ -167,8 +182,17 @@ class Box {
     }
     makeLoan(loan) {
         this.verifyIfMemberIsOnThisBox(loan._member);
-        this.loans.push(loan);
+        if (loan._isDisbursed) {
+            return;
+        }
+        if (loan.value > this.balance) {
+            throw new DomainError_1.default('box does not have enough funds');
+        }
+        if (!this.loans.some(item => item.UUID === loan.UUID)) {
+            this.loans.push(loan);
+        }
         this.currentBalance = DecimalValue_1.DecimalValue.from(this.currentBalance.val - loan.value);
+        loan.markAsDisbursed();
     }
     memberIsOnThisBox(member) {
         const exists = this.members.map(m => m.memberName).includes(member.memberName);
diff --git a/node_modules/caixinha-core/dist/src/boxes/box.types.d.ts b/node_modules/caixinha-core/dist/src/boxes/box.types.d.ts
index 05ee54a..833845a 100644
--- a/node_modules/caixinha-core/dist/src/boxes/box.types.d.ts
+++ b/node_modules/caixinha-core/dist/src/boxes/box.types.d.ts
@@ -56,6 +56,8 @@ export interface LoanBoxJson {
     isPaidOff?: boolean;
     installments?: number;
     refusedReason?: RefusedReasonJson;
+    disbursed?: boolean;
+    originLoanUid?: string;
 }
 export interface ValueRequested {
     value: number;
diff --git a/node_modules/caixinha-core/dist/src/loans/Loan.d.ts b/node_modules/caixinha-core/dist/src/loans/Loan.d.ts
index b4e8999..5254848 100644
--- a/node_modules/caixinha-core/dist/src/loans/Loan.d.ts
+++ b/node_modules/caixinha-core/dist/src/loans/Loan.d.ts
@@ -23,7 +23,10 @@ export declare class Loan {
     private isPaidOff;
     private installments;
     private refusedReason;
+    private disbursed;
+    private readonly originLoanUid?;
     constructor(input: CreateLoanInput);
+    static createRenegotiated(input: CreateLoanInput, approvers: Member[]): Loan;
     static fromBox(input: FromBoxInput): Loan;
     refuse(reason: string, member: Member): boolean;
     memberCanCanceledThisLoan(member: Member): boolean;
@@ -53,4 +56,6 @@ export declare class Loan {
     calculateOverdueDays(today?: Date, timeZone?: string): number;
     get isOverdue(): boolean;
     get totalPayments(): number;
+    get _isDisbursed(): boolean;
+    markAsDisbursed(): void;
 }
diff --git a/node_modules/caixinha-core/dist/src/loans/Loan.js b/node_modules/caixinha-core/dist/src/loans/Loan.js
index c6787fc..40ae339 100644
--- a/node_modules/caixinha-core/dist/src/loans/Loan.js
+++ b/node_modules/caixinha-core/dist/src/loans/Loan.js
@@ -22,6 +22,8 @@ class Loan {
         this.payments = [];
         this.uid = (0, utils_1.generateUUID)();
         this.installments = input.installments;
+        this.disbursed = false;
+        this.originLoanUid = input.originLoanUid;
         if (!input.skipValidate)
             this.validate(true);
         this.memberName = this.member.memberName;
@@ -29,9 +31,18 @@ class Loan {
         this.listOfMembersWhoHaveAlreadyApproved = [];
         this.generateBillingDates();
         this.calculateTotalValue();
+        this.calculateRemainingAmount();
+    }
+    static createRenegotiated(input, approvers) {
+        const loan = new Loan(Object.assign(Object.assign({}, input), { skipValidate: true }));
+        loan.approved = true;
+        loan.approvals = approvers.length;
+        loan.listOfMembersWhoHaveAlreadyApproved = [...approvers];
+        loan.disbursed = false;
+        return loan;
     }
     static fromBox(input) {
-        var _a, _b;
+        var _a, _b, _c;
         const l = new Loan({
             member: input.member,
             valueRequested: input.valueRequested.value,
@@ -40,7 +51,8 @@ class Loan {
             box: input.box,
             description: input.description,
             skipValidate: true,
-            date: (0, utils_1.stringToDate)(input.date)
+            date: (0, utils_1.stringToDate)(input.date),
+            originLoanUid: input.originLoanUid
         });
         l.payments = input.payments;
         l.installments = input.installments;
@@ -52,6 +64,7 @@ class Loan {
         l.billingDates = input.billingDates.map(billDate => (new Date(billDate)));
         l.totalValue = DecimalValue_1.DecimalValue.from(((_a = input === null || input === void 0 ? void 0 : input.totalValue) === null || _a === void 0 ? void 0 : _a.value) || 0);
         l.remainingAmount = DecimalValue_1.DecimalValue.from(((_b = input === null || input === void 0 ? void 0 : input.remainingAmount) === null || _b === void 0 ? void 0 : _b.value) || 0);
+        l.disbursed = (_c = input.disbursed) !== null && _c !== void 0 ? _c : input.approved;
         if (input.refusedReason) {
             l.refusedReason = RefusedReason_1.RefusedReason.fromJson(input.refusedReason);
         }
@@ -255,5 +268,11 @@ class Loan {
     get totalPayments() {
         return this.payments.reduce((acumulator, payment) => acumulator + payment._value, 0);
     }
+    get _isDisbursed() {
+        return this.disbursed;
+    }
+    markAsDisbursed() {
+        this.disbursed = true;
+    }
 }
 exports.Loan = Loan;
diff --git a/node_modules/caixinha-core/dist/src/loans/loan.types.d.ts b/node_modules/caixinha-core/dist/src/loans/loan.types.d.ts
index 5a54213..9a7ecd2 100644
--- a/node_modules/caixinha-core/dist/src/loans/loan.types.d.ts
+++ b/node_modules/caixinha-core/dist/src/loans/loan.types.d.ts
@@ -33,6 +33,8 @@ export interface FromBoxInput {
     isPaidOff?: boolean;
     installments?: number;
     refusedReason?: RefusedReasonJson;
+    disbursed?: boolean;
+    originLoanUid?: string;
 }
 export interface RefusedReasonJson {
     reason: string;
@@ -49,4 +51,5 @@ export interface CreateLoanInput {
     fees?: number;
     skipValidate?: boolean;
     installments?: number;
+    originLoanUid?: string;
 }
diff --git a/node_modules/caixinha-core/dist/src/renegotiations/useCase/AcceptRenegotiation.js b/node_modules/caixinha-core/dist/src/renegotiations/useCase/AcceptRenegotiation.js
index aaf947b..7791334 100644
--- a/node_modules/caixinha-core/dist/src/renegotiations/useCase/AcceptRenegotiation.js
+++ b/node_modules/caixinha-core/dist/src/renegotiations/useCase/AcceptRenegotiation.js
@@ -15,10 +15,11 @@ function AcceptRenegotiation(box, renegotiation, member, input) {
         box,
         description: `Renegotiation ${today.getDate()}/${today.getMonth()}/${today.getFullYear()}`,
         installments: input.installmentOptions,
-        skipValidate: true
+        skipValidate: true,
+        originLoanUid: renegotiation.originLoan.UUID
     };
-    const newLoanEntity = createLoanAndAprove(newLoan);
-    removeOldLoanFromBox(renegotiation, box);
+    const newLoanEntity = Loan_1.Loan.createRenegotiated(newLoan, box._members);
+    box.replaceLoanForRenegotiation(renegotiation.originLoan.UUID, newLoanEntity);
     renegotiation.complete(newLoanEntity);
     return {
         reneg: renegotiation,
@@ -26,13 +27,6 @@ function AcceptRenegotiation(box, renegotiation, member, input) {
     };
 }
 exports.default = AcceptRenegotiation;
-function createLoanAndAprove(input) {
-    const loan = new Loan_1.Loan(input);
-    input.box._members.forEach(m => {
-        loan.addApprove(m);
-    });
-    return loan;
-}
 function applyValidations(renegotiation, member, input) {
     if (input.installmentOptions <= 0 || input.newTotalValue <= 0) {
         throw new DomainError_1.default("Invalid input");
@@ -41,7 +35,3 @@ function applyValidations(renegotiation, member, input) {
         throw new DomainError_1.default("You are not the owner of this loan");
     }
 }
-function removeOldLoanFromBox(renegotiation, box) {
-    const uuid = renegotiation.originLoan.UUID;
-    box.removeLoan(uuid);
-}
